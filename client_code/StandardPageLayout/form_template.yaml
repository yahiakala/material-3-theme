properties:
- name: navigation_rail_collapse_to
  type: enum
  default_value: Bottom app bar
  default_binding_prop: true
  options: [Bottom app bar, Navigation drawer]
  description: How should the side navigation rail collapse on mobile?
- {name: navigation_rail_color, type: string, default_value: ''}
- {name: background_color, type: string, default_value: ''}
- {name: text_color, type: string, default_value: ''}
is_package: true
slots:
  slot_1:
    index: 0
    set_layout_properties: {slot: content}
    target: {type: container, name: ''}
  slot_2:
    index: 0
    set_layout_properties: {slot: navigation-rail-content}
    target: {type: container, name: ''}
custom_component: true
components: []
container:
  type: HtmlTemplate
  properties: {html: "<div anvil-name=\"navigation-rail\" class=\"navigation-rail\"\
      >\n  <div class=\"navigation-rail-content\" anvil-slot=\"navigation-rail-content\"\
      >\n      <p anvil-if-slot-empty=\"navigation-rail-content\" style=\"font-style:\
      \ italic;\">Add navigation links here</p>\n  </div>\n</div>\n\n<div class=\"\
      top-app-bar\">\n  <div class=\"navigation-drawer-btn\">\n    <i class=\"fa fa-bars\"\
      ></i>\n  </div>\n</div>\n\n<div class=\"navigation-drawer-scrim\"></div>\n\n\
      <div class=\"content\" anvil-slot=\"content\">\n  <p anvil-if-slot-empty=\"\
      content\" style=\"font-style: italic;\">Add content here</p>\n</div>\n\n<script>\n\
      \    var appBar = $('.top-app-bar')\n    var navDrawerBtn = $('.navigation-drawer-btn')\n\
      \    var navRailContent = $('.navigation-rail-content')\n\n    document.addEventListener('scroll',\
      \ function() {\n      console.log('scrolled')\n    if (appBar.classList.contains('scrolled'))\
      \ {\n      if (window.scrollY === 0) {\n        appBar.classList.remove('scrolled')\n\
      \      }\n    }\n    else {\n      appBar.classList.add('scrolled')\n    }\n\
      \  });\n  \n  navDrawerBtn.on('click', function() {\n    if (navRailContent.is('.shown'))\
      \ {\n      console.log('shown...now hiding')\n      $('.navigation-drawer-scrim').css({display:\
      \ 'none'})\n      setTimeout(() => {navRailContent.animate({left:\"-100%\"},\
      \ 200, function () {\n      })}, 100)\n      setTimeout(() => {navRailContent.removeClass('shown')},\
      \ 400)\n    } else {\n      console.log('hidden...now showing')\n      $('.navigation-drawer-scrim').css({display:\
      \ 'block'})\n      navRailContent.addClass('shown')\n      setTimeout(() =>\
      \ {navRailContent.animate({left:\"0px\"}, 200,  function () {\n      })}, 100)\
      \      \n    }\n  } )\n\n</script>\n"}
